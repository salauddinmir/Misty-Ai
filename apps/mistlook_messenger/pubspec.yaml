name: mistlook_messenger
description: "Mistlook Messenger — a privacy-first, offline-ready messaging client."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.13.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.6.1
  go_router: ^16.2.0
  supabase_flutter: ^2.10.0
  shared_preferences: ^2.5.3
  uuid: ^4.5.1
  intl: ^0.20.2
  http: ^1.5.0
  web_socket_channel: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
